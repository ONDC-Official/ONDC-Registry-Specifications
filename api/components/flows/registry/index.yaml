summary: Registry sample flow
details:
  - description:
      The illustrative flow depicts the comprehensive process of Registry
      , showcasing the sequential steps and interactions involved in
      a Registry Process.
    mermaid: |-
      sequenceDiagram
        title Initializing Registry
        participant Network Participant
        participant Registry
        participant Backend
        Network Participant->>Registry: subscribe
        Registry ->> Backend : domain validation
        Registry ->> Backend : SSL (OCSP) validation
        Registry ->> Backend : signed key verification
        Backend ->> Registry : encryped Challenged_string

        Registry->>Network Participant: (callback_url/on_subscribe) challenge 
        Network Participant ->> Registry : return decrypted challege string 
        Network Participant->>Registry: decrypt challenge
        Registry ->> Backend : compair both the string 
        Registry ->> Backend : equal/ Not equal
        Registry-->>Network Participant: Ack

steps:
  - summary: Registry
    api: subscribe
    details:
      - description: subscribe
        mermaid: >-
          
    example:
      value:
        $ref: "../../examples/registry/subscribe/subscribe.yaml"
  - summary:  On-subscribe
    api: on_subscribe
    details:
      - description: on_subscribe
        mermaid: >-
            
    example:
      value:
        $ref: "../../examples/registry/subscribe/subscribe.yaml"      
